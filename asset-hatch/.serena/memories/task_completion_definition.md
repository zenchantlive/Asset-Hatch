# Task Completion Definition

Before marking a task as complete:
1. Ensure the code compiles (`bun run build` or check dev server).
2. Run linting (`bun run lint`) and fix errors.
3. Run tests (`bun run test`) to ensure no regressions.
4. Verify changes manually if UI related.
5. Create or update relevant documentation if needed.
