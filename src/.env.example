# ============================================
# ASSET HATCH - Environment Configuration
# ============================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# ============================================
# REQUIRED - App will not start without these
# ============================================

# Session encryption secret
# Generate with: openssl rand -base64 32
# CRITICAL: Keep this secret and never commit it
AUTH_SECRET=""

# ============================================
# DATABASE CONFIGURATION
# ============================================

# For LOCAL DEVELOPMENT (SQLite):
# Creates a local dev.db file in the src/ folder
DATABASE_URL="file:./dev.db"

# For VERCEL PRODUCTION (PostgreSQL):
# Vercel auto-populates these when you add Vercel Postgres storage
# DO NOT set these manually - Vercel handles them automatically
# POSTGRES_PRISMA_URL=""  # Auto-set by Vercel (connection pooling)
# DATABASE_URL=""          # Auto-set by Vercel (overrides SQLite in production)

# ============================================
# GITHUB OAUTH - Required for user login
# ============================================
# Create an OAuth App at: https://github.com/settings/developers
#
# For local development:
#   Homepage URL: http://localhost:3000
#   Callback URL: http://localhost:3000/api/auth/callback/github
#
# For production:
#   Homepage URL: https://your-domain.com
#   Callback URL: https://your-domain.com/api/auth/callback/github

AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# ============================================
# OPENROUTER - Required for AI features
# ============================================
# Get your API key at: https://openrouter.ai/keys
#
# Pricing (approximate):
#   Chat (Gemini): ~$0.001 per message
#   Image (Flux):  ~$0.03-0.05 per image
#
# Monthly cost depends on usage:
#   Light use: $5-10/month
#   Heavy use: $20-50/month

OPENROUTER_API_KEY=""

# ============================================
# TRIPO3D - Optional for 3D Mode
# ============================================
# Get your API key at: https://platform.tripo3d.ai
#
# Pricing (approximate):
#   Text-to-3D Model: ~$0.20 per model
#   Auto-rigging:     ~$0.10 per rig
#   Animation:        ~$0.05 per animation
#
# Monthly cost depends on usage:
#   Light use (testing): $5-10/month
#   Production use:      $20-100/month
#
# Key format: tsk_***

TRIPO_API_KEY=""

# ============================================
# OPTIONAL - Advanced Configuration
# ============================================

# Your app's public URL (only needed for production)
# NEXT_PUBLIC_APP_URL="https://your-domain.com"

# Enable debug logging for AI SDK
# AI_SDK_DEBUG="true"

# Maximum number of concurrent image generations
# MAX_CONCURRENT_GENERATIONS="3"
