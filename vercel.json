{
    "buildCommand": "bun install && bunx prisma generate && bunx prisma db push && bun run build",
    "outputDirectory": "src/.next",
    "installCommand": "bun install",
    "framework": "nextjs",
    "regions": [
        "iad1"
    ]
}